# vim: ft=sh

run="dr --prefix resume"

$run --workspace 1 --focus -- zen-browser
$run -- Telegram
$run -- signal-desktop
$run -- vesktop --wayland
$run -- $TERMINAL -e fish -c 'tn monitors'
$run --name spotify -- gtk-launch spotify-adblock
$run --tile --workspace 8 -- $TERMINAL -e fish -c 'wiremix --tab output'
$run --workspace 9 -- betterbird
