# vim: ft=hyprlang

# Left
bind = $mod, h, movefocus, l
bind = $mod $ctrl, h, movewindoworgroup, l
bind = $mod $ctrl $shift, h, movecurrentworkspacetomonitor, l

# Down
bind = $mod, j, movefocus, d
bind = $mod $ctrl, j, movewindow, d
bind = $mod $shift, j, movetoworkspace, emptym

# Up
bind = $mod, k, movefocus, u
bind = $mod $ctrl, k, movewindow, u

# Right
bind = $mod, l, movefocus, r
bind = $mod $ctrl, l, movewindoworgroup, r
bind = $mod $ctrl $shift, l, movecurrentworkspacetomonitor, r
