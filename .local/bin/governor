#!/bin/sh

governors=$(sed 's/\s/\n/g' /sys/devices/system/cpu/cpu0/cpufreq/scaling_available_governors)
governor=$(echo "$governors" | rofi -dmenu)

if [ -n "$governor" ]; then
  pkexec cpupower frequency-set -g "$governor"
fi
