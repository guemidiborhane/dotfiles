# -*- mode: bash -*-
# vi: set ft=bash :

function _rspec_command () {
  if [ -e "bin/rspec" ]; then
    bin/rspec $@
  fi
}

function t() {
  TMUX= tmux new-session -d -s $(basename $(pwd))
  tmux switch-client -t $(basename $(pwd))
}