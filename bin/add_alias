#!/bin/bash

if [[ "$1" && "$2" ]]
then
    printf "\nalias $1=\"$2\"" >> ~/.aliases
    source ~/.zshrc
else
    echo "Error: missing required parameters."
    echo "Usage: "
    echo "  add_alias name command"
fi    
